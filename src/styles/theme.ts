import { createTheme } from '@nextui-org/react'

export const darkTheme = createTheme({
  type: 'dark',
  theme: {
    colors: {
      gradient:
        'linear-gradient(112deg, var(--nextui-colors-red400) -63.59%, var(--nextui-colors-red600) -20.3%, var(--nextui-colors-red400) 70.46%)',
    },
  },
})
